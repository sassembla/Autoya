using System;
using UnityEngine;

namespace AutoyaFramework.AssetBundle {
    public class AssetBundleReader {
        public T LoadAssetBundleOfResource <T> (string resourceName, Action<T> loaded, Action failed) where T : UnityEngine.Object {
            // return new T();
            throw new Exception("");
        }
        
    }
}